# Next.js
web/.next/
web/out/
# Auto-regenerated by Next.js on every build — not ours to track
web/next-env.d.ts

# Dependencies
web/node_modules/

# Environment variables — never commit these
web/.env
web/.env.local
web/.env.*.local

# Build output
web/build/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.suo
*.user

# Large/source video files — only the optimised build is tracked
web/public/video/who_we_serve.mp4
web/public/video/who_we_serve_2k.mp4
web/public/video/hero.mp4